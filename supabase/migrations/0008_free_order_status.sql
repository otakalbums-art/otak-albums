-- ============================================================================
-- Статус "Безкоштовно" — альбом видано без оплати (напр. класному керівнику)
-- ============================================================================
-- Виключається з фінансової статистики на дашборді (apps/admin/app/page.tsx) —
-- ні в потенційний дохід, ні в оплачено/собівартість, на відміну від решти
-- статусів.
-- ============================================================================

alter table students drop constraint students_order_status_check;
alter table students add constraint students_order_status_check
  check (order_status in ('not_ordered', 'ordered', 'partially_paid', 'paid', 'free'));
